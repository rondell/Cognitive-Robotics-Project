


#include "cliio.h"

#include "num.h"


/* Dopo chiederà in ingresso : num *Phi , num *InputImage , num *Output, int Width , int Height, int Num channel(last three are inside the Image output)*/
int Active_Contour (num *Contour, image *ImageInput,num *OutPut);